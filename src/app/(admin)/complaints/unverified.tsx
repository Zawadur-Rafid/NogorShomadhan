// src/app/(admin)/complaints/unverified.tsx

import { View, Text } from 'react-native';

export default function Unverified() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>unverified Screen</Text>
    </View>
  );
}
